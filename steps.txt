rewrite code in JavaScript
store chosen part in separate array, and remove chosen part from main array
write code to reset the game. merge the chosen parts array with the main array
make a UI for the code
make chosen parts visible to user, so that they know what parts they can choose from. separate by category (head, legs, etc)

